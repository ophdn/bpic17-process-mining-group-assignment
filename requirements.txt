# Direct dependencies only (transitive deps are resolved by pip).

# Simulation engine + data-driven components
joblib==1.5.3
numpy==2.5.0
pandas==3.0.3
pm4py==2.7.23.1
scikit-learn==1.9.0
scipy==1.18.0

# Resource analysis notebooks (Sections 1.6 / 1.7)
ipykernel==7.2.0
matplotlib==3.10.9
seaborn==0.13.2
networkx==3.6.1   # §1.7 resource-map (force-directed layout of the org groups)

# Tests. The suite is unittest-based and runs on the stdlib
# (`python -m unittest discover -s tests`), so pytest is optional -- it is here
# for the nicer output and `-k` selection, and runs the same TestCases unchanged.
pytest==9.0.1
