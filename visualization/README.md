# Notebook visualizations

The analysis notebooks write report-ready figures to this directory in both
PDF and SVG format through `analysis.tum_style.save_figure`. The palette uses
the exact RGB definitions from the companion report repository's
`tum/tumcolors.sty`.

Run a notebook from either the repository root or `notebooks/`; the export
helper resolves this directory automatically.
